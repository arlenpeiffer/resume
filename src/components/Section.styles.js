import { css } from "@emotion/core"

const styles = css`
  h3 {
    color: var(--accentColor);
    margin-top: 5em;
    margin-bottom: 2em;
  }

  li {
    margin-bottom: 2em;
  }
`

export default styles
